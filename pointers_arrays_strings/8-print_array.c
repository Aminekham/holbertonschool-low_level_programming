#include "main.h"
/**
 * 
 * 
 */
void print_array(int *a, int n)
{
	int i;
	for (i = 0; i <= n - 1; i++)
	{
		if (i == n - 1)
		{
			printf(" %d", *a);
		}
		else if 
		{
			print("%d,", *a)
		}
		else
		{
	    	printf(" %d,", *a);
			a++;
		}
	}
	printf("\n");
}