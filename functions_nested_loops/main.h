#include <stdio.h>
/**
 * print_alphabet - check the code
 *
 * Return: void not 0.
 */

void print_alphabet(void)
{
	int i;

	for (i = 97; i < 123; i++)
{
		putchar(i);
}
	putchar('\n');
	return (void);
}
