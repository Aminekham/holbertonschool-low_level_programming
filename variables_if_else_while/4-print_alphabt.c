#include <stdio.h>
/**
 * main - Entry point for c program
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 97; i < 101; ++i)
{
		putchar(i("\n"));
}
	for (i =102 ; i < 123; ++i)
		putchar(i("\n"));
	return (0);
}
